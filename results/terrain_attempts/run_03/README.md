# Failed development run

Aborted after an overconfident semantic water prediction blocked the launch pad. The subsequent implementation keeps learned semantics as an inflated risk cost, not a hard geometric label.
